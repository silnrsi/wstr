---
title: Data presentation testing
description: Testing options for constructing pages from and containing data
sidebar:
  order: 9800.4
  label: Data Test VG
lastUpdated: 2025-07-23
---

import DataTableTest from '../../../components/DataTableTest.astro';
import DataTableTestItem from '../../../components/DataTableTestItem.astro';
import testdata from '../../../data/testdata.json';	

## List of data defined in component
Here the data is imported from the `testdata.json` file and structured in the component. This page only references the component as a whole.
<DataTableTest />

## Individual data elements defined in component
Here the data is imported on this page. The component is minimal (and probably unnecessary) and only shows how one item is to be displayed.
{	testdata.map((category) => (
	<div>
		<h3>{category.Category}</h3>
		<ul>
			{category.Items.map((item) => (
				<DataTableTestItem item={item} />
			))}
		</ul>
	</div>
))
}


This is the start of the normal markdown.

